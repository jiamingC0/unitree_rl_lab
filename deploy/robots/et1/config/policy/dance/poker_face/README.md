PokerFace policy placeholder.

Expected structure:
- exported/policy.onnx
- params/deploy.yaml
- params/poker_face.et1trk

Enable the `PokerFace` FSM block in `config.yaml` after these files are added.
